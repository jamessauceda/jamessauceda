{
  "name": "wedding-website",
  "version": "1.0.0",
  "description": "A wedding website with loads of features.",
  "main": "npmfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "aws s3 sync . s3://wedding-jamessauceda --exclude 'node_modules/_'"
  },
  "devDependencies": {
    "aws-sdk": "^2.427.0",
    "gulp": "^3.9.1",
    "gulp-rename": "^1.2.2",
    "gulp-sass": "^4.0.2",
    "gulp-uglify": "^3.0.0"
  },
  "dependencies": {
    "animate.css": "^3.5.2",
    "aws-sdk": "^2.399.0",
    "font-awesome": "^4.7.0",
    "gulp-uglify-es": "^1.0.4",
    "waypoints": "^4.0.1"
  }
}
